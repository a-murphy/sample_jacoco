language: java

jdk:
  - oraclejdk8
  
env:
  - ONE=true
  - TWO=true

build:
  ci:
    - mkdir -p shippable/codecoverage
    - mvn install
    - cp -r target shippable/codecoverage
    - if [ $ONE = true ]; then fail; fi
